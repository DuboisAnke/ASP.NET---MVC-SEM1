@model IEnumerable<ProjectCWeb_DuboisAnke_2ProA.Models.Academiejaar>

@{
    ViewData["Title"] = "Index";
}

<link rel="stylesheet" href="css/personalstyling.css" />
<link rel="stylesheet" href="~/css/footerstyle.css" />

<br />
<h1>ACADEMIEJAREN</h1>
<hr />
<p>
    <a asp-action="Create" class="text-link p-2">Nieuw academiejaar aanmaken</a>
</p>
<hr />
<table class="personal-table">
    <thead>
        <tr>
            <th>
                <label>STARTDATUM</label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDatum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AcademieJaarId">Bewerk</a> |
                <a asp-action="Details" asp-route-id="@item.AcademieJaarId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AcademieJaarId">Verwijder</a>
            </td>
        </tr>
}
    </tbody>
</table>
